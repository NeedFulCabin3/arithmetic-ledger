# arithmetic-ledger
A Tkinter desktop calculator that evaluates typed or clicked arithmetic expressions through Python's eval() with builtins disabled, keeps a running session history in a collapsible Listbox panel, accepts keyboard input via event binding, and computes square roots, squares, and exponents alongside the four standard operators.
